#!/usr/bin/env bash
# bin/detect <build-dir>

set -o errexit
set -o errtrace
set -o verbose
set -o xtrace
set

build_dir=$1

if [ ! -f "$build_dir/static.json" ]; then
    exit 1
fi

echo 'Static HTML'

set
exit 0
